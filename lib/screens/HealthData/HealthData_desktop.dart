import 'package:flutter/material.dart';

class HealthDataDesktop extends StatelessWidget {
  const HealthDataDesktop({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(

    );
  }
}